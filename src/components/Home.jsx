import { View, Text, TouchableHighlight } from "react-native"
import { redirect } from "react-router-native";

const Home = () => {
    return(
        <View style={{}}>
            <Text>Home</Text>
        </View>
    )
}

export default Home;